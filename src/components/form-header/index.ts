import { FormHeaderDesc } from "./form-header-desc";
import { FormHeaderRoot } from "./form-header-root";
import { FormHeaderTitle } from "./form-header-title";

export const FormHeader = {
  Root: FormHeaderRoot,
  Title: FormHeaderTitle,
  Desc: FormHeaderDesc,
}